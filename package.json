{
  "name": "pp-ui-core",
  "version": "1.0.0",
  "description": "CDN-based UI framework for Power Pages",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build",
    "preview": "npm run build && npx http-server -c-1 -p 8080 ."
  }
}